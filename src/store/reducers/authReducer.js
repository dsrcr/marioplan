/* eslint-disable default-param-last */

/**
 * The authReducer function is a reducer function that returns the current state of the application's authentication.
 *
 *
 * @param state=initialState Used to Set the initial state of this reducer.
 * @param action Used to Determine what action is being taken.
 * @return The initialstate object.
 *
 * @doc-author dsrcr
 */
const initialState = {};
const authReducer = (state = initialState, action) => {
    return state;
};

export default authReducer;
